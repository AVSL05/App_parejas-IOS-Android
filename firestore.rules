rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura y escritura para usuarios autenticados (reglas temporales para debugging)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
